[project]
name = "context-harness-mcp-scripts"
version = "0.1.0"
description = "One-shot migration and seed tooling for context-harness-mcp."
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary] >= 3.2",
  "pgvector >= 0.3.6",
  "chromadb >= 0.5.0",
  "click >= 8.1",
]

[tool.uv]
package = false
